
<div class="modal modal-sheet position-static d-block p-4 py-md-5" tabindex="-1" role="dialog" id="modalSignin">
  <div class="modal-dialog" role="document">
    <div class="modal-content rounded-4 shadow">
<div class="modal-header p-5 pb-4 border-bottom-0">
  <h1 class="fw-bold mb-0 fs-2">Exclusão de espaço</h1>
</div>

  <div class="modal-body p-5 pt-0"  id="divForm">
    <form id="form" onsubmit="enviaForm(event)"  > 
      <div  class="form-floating mb-3">
        <input class="form-control rounded-3" type="text" id="descricao" name="descricao" required placeholder=""/>
        <label id="lblDesc">Descrição</label>
      </div>
      <div  class="form-floating mb-3">
        <input class="form-control rounded-3" type="text" id="inicio" name="inicio" required placeholder =""/>
        <label id="lblInicio">Inicio</label>
      </div>
      <div  class="form-floating mb-3">
        <input class="form-control rounded-3" type="text" id="fim" name="fim" required placeholder =""/>
        <label id="lblFim">Fim</label>
      </div>
      <div id="divErro"></div>

      <div style="display: flex;flex-direction: row; justify-content: space-around; margin-top: 2em;">
         <button type="button"  class="btn btn-outline-dark btn-md" onclick="irParaHome()">Home</button>
         <button  class="btn btn-outline-dark btn-md" type="submit">Excluir</button>
      </div>     
    </form>
  </div>
</div>
</div>
<script>
  verificaLogado();
  async function lerRes(){
    const id = "<%= id %>";
    await lerReserva(id)
  }
  lerRes()
 async function enviaForm(event){
  event.preventDefault()
  const id = "<%= id %>";
  await deletReserva(event,id)
 }
</script>